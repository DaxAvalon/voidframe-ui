// Phase 15 — light theme (inverted surface/text hierarchy).

import { lightTheme as legacyLight } from "../tokens";
import type { VoidframeTokens } from "../tokens";

export const lightTheme: VoidframeTokens = legacyLight;
