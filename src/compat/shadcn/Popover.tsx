"use client";

// shadcn-shaped Popover — backed by voidframe's PopoverV2 compound.

import {
  PopoverRoot,
  PopoverTrigger,
  PopoverContent,
} from "../../components/Popovers";

export const Popover = PopoverRoot;
export { PopoverTrigger, PopoverContent };
