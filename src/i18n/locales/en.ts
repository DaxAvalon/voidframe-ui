import { enMessages } from "../messages";
import type { LocalePack } from "./types";

export const en: LocalePack = {
  locale: "en",
  direction: "ltr",
  firstDayOfWeek: 0,
  messages: enMessages,
};
