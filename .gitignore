node_modules/
dist/
coverage/

# VS Code extension bundles are build artifacts, produced by
# `npm run vscode:package`. The snippet JSON they consume IS committed;
# the .vsix is not.
tools/vscode-voidframe/*.vsix

# Editor
.DS_Store
.idea/
.vscode/*
!.vscode/settings.json

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Playwright
playwright-report/
test-results/

# Internal development / planning docs. Kept locally but not pushed to the
# public mirror — consumer-facing history lives in CHANGELOG.md, and the
# contributor workflow lives in CONTRIBUTING.md.
plans/
VoidFrameImplementationPlan.md

# Claude Code project-level settings (permissions allowlist).
.claude/
