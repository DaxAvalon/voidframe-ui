Network call sites (production only). Excludes __tests__.

── fetch ─────────────────────────────────────────────────────
src/components/MediaPlayer.tsx:672      fetch(audioUrl) — `audioUrl` is a VideoPlayer prop (consumer-supplied).
                                        Used only to decode audio for waveform visualization. No user-supplied
                                        body/headers. SSRF boundary is whatever the consumer's page can reach
                                        cross-origin; browser enforces CORS.
                                        Classification: CONSUMER-PROVIDED URL, trusted-consumer.

── XMLHttpRequest ────────────────────────────────────────────
(none in src/)

── WebSocket ─────────────────────────────────────────────────
src/hooks/useWebSocket.ts:104,105       new WebSocket(url, protocols) — url/protocols are hook arguments
                                        passed by the consumer. Library never derives the URL from user input.

── EventSource ───────────────────────────────────────────────
src/hooks/useEventSource.ts:77          new EventSource(url, { withCredentials }) — url is hook arg.

── sendBeacon / MessageChannel / BroadcastChannel / SharedWorker / postMessage listener ──
(none in src/)

── postMessage listeners ─────────────────────────────────────
(none in src/) — library does not subscribe to "message" events; no origin validation needed.

Result: Every network endpoint is consumer-provided. The library does not originate requests to
attacker-controlled URLs. MediaPlayer's fetch is the only implicit request, gated on a prop.
