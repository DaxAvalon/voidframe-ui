HTML sink inventory — grep patterns: innerHTML=, dangerouslySetInnerHTML, outerHTML=, setAttribute(
Collected: 2026-04-17.
Excludes __tests__ and *.test.* files.

── dangerouslySetInnerHTML ──────────────────────────────────
(none) — zero occurrences in src/ outside tests

── innerHTML / outerHTML (production) ───────────────────────
src/components/RichEmbed.tsx:47           writeHTML helper: `(el as Record<string, string>)["innerHTML"] = html;`
                                          call site line 137 uses sanitizeHtml(state.svg, "svg")
src/components/Viewers/CodeBlock.tsx:16   writeHTML helper same pattern; call site line 36 in HTMLPane —
                                          input is `html` prop fed by caller's `highlight(code, language)` return value.
src/components/RichTextEditor.tsx (indirect): writeHTML helper used at line 224/246 — input passed through sanitize() (DOMPurify).
src/components/Print.tsx:131-168          iframe.contentDocument.write(...) — writes outerHTML of same-origin elements;
                                          title string escapes via escapeHTML (line 147).

── setAttribute (dangerous-subset only) ─────────────────────
No src/ production code sets the dangerous-subset attributes (`href`, `src`,
`srcdoc`, `action`, `formaction`, `xlink:href`, `style`, `on*`, `srcset`,
`<object data>`) via setAttribute. Every hit is an aria-*, data-*, role,
draggable, or tabindex — library-controlled literal values.

Full setAttribute surface for record:
src/components/DragDrop.tsx:185           setAttribute("draggable", "true")                — LIBRARY-STRING
src/utils/portalContainer.ts:28           setAttribute("data-vf-portal", "")                — LIBRARY-STRING
src/utils/announce.ts:14-16               aria-live / aria-atomic / role (literal values)   — LIBRARY-STRING
src/utils/sanitizeHtml.ts:91              setAttribute("rel", "noreferrer noopener")        — LIBRARY-STRING (in DOMPurify hook)
src/components/Print.tsx:138              setAttribute("aria-hidden", "true")               — LIBRARY-STRING
src/hooks/useAnnouncer.ts:36-38           aria-live / aria-atomic / data-vf-announcer       — LIBRARY-STRING

Result: zero HTML sinks accept unsanitized user data in production.
