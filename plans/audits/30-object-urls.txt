Blob / URL.createObjectURL sites (production only). Excludes __tests__.

src/components/DataGrid.tsx:1208-1209    new Blob([content], { type: mime }); URL.createObjectURL(blob)
                                         — CSV/JSON/TSV export; `mime` comes from the component (one of
                                         "text/csv", "application/json", "text/plain"). LIBRARY-CONTROLLED.

src/components/ImageCropper.tsx:142      new Blob flow for user-selected File (drag/drop). MIME = file.type
                                         (browser-determined). The resulting objectURL is assigned to
                                         <img src>; browsers will not execute <img src="blob:..."> as script.
                                         LIBRARY-CONTROLLED MIME for rendering; input gate is the file picker.

src/components/ImageCropper.tsx:235      new Blob() as empty-canvas fallback — LITERAL.

src/components/Viewers/CodeBlock.tsx:120-121    new Blob([code], { type: "text/plain" }) for download.
                                                MIME literal. LIBRARY-CONTROLLED.

src/charts/primitives/ChartFrame.tsx:103-104    new Blob([svgString], { type: "image/svg+xml;charset=utf-8" })
                                                for chart SVG export. SVG string is library-rendered chart
                                                output; MIME literal. LIBRARY-CONTROLLED.

No user-supplied MIME type reaches a Blob constructor. Object-URLs are only used as <img src> /
<a href> download targets; none are navigated to as a top-level document.
