setTimeout / setInterval call sites (production only). Every first-arg is a function literal.

src/hooks/useLongPress.ts:32                  setTimeout(() => { … })
src/hooks/useThrottle.ts:20                   setTimeout(() => { … })
src/hooks/useStopwatch.ts:32                  setInterval(() => { … })
src/components/Carousel.tsx:146               setInterval(() => setIndex(indexRef.current+1), interval)
src/hooks/useDebouncedCallback.ts:50          setTimeout(() => { … })
src/hooks/useCountdown.ts:52                  setInterval(() => { … })
src/hooks/useWebSocket.ts:122                 setTimeout(() => { connect() }, retryInterval)
src/primitives/LiveRegion.tsx:31              setTimeout(() => setCurrent(""), clearAfter)
src/hooks/index.ts:254                        setTimeout(() => setDebounced(value), delay)
src/hooks/index.ts:313                        setInterval(() => savedCallback.current(), delay)
src/hooks/index.ts:372                        setTimeout(() => setCopied(false), resetDelay)
src/hooks/useFocusReturn.ts:27                setTimeout(() => el.focus(), 0)
src/primitives/FocusScope.tsx:71              setTimeout(() => el.focus(), 0)
src/hooks/useAnnouncer.ts:57                  setTimeout(() => { … })
src/components/Animations.tsx:162-174         setTimeout(tick, stepMs) / setTimeout(() => {…})
src/components/Utility.tsx:48,117             setTimeout(() => setCopied(false), …)
src/components/ChatComposer.tsx:407           setTimeout(() => setRecording(false), maxDuration)
src/components/DrawerCompound.tsx:186         setTimeout(() => el.focus(), 0)
src/hooks/useEventSource.ts:93                setTimeout(() => { connect() }, retryInterval)
src/components/ToastSystem.tsx:234,254        setTimeout(() => { … })
src/utils/announce.ts:55,59                   setTimeout(() => { … })
src/components/Popovers.tsx:303,307,448,452   setTimeout(() => { … })
src/components/Chat/Message.tsx:326           setInterval(() => { … })
src/components/TreeView.tsx:261               setTimeout(() => { … })
src/components/TimeDisplays.tsx:168,289       setInterval(…)
src/components/Print.tsx:159,166              setTimeout(() => {…}, 500) / setTimeout(cleanup, 10000)
src/components/Viewers/CodeBlock.tsx:113      setTimeout(() => setCopied(false), 1500)
src/components/CopyButton.tsx:66              setTimeout(() => { … })
src/components/Dialog.tsx:222                 setTimeout(() => el.focus(), 0)
src/hooks/useThrottledCallback.ts:44,55       setTimeout(() => { … })
src/hooks/useIdle.ts:26                       setTimeout(() => setIdle(true), timeout)
src/hooks/useTimeout.ts:16                    setTimeout(() => savedCallback.current(), delay)

All FUNCTION-ARG. No string-form timers. Safe.
