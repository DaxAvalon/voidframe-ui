Persistence call sites — localStorage / sessionStorage / document.cookie. Excludes __tests__.

── localStorage ──────────────────────────────────────────────
src/hooks/index.ts:279,290            useLocalStorage generic hook. Key is user-provided (consumer passes it).
                                      Stored content is consumer JSON.stringify'd state. Documented generic.

src/hooks/useThemePersistence.ts:44,51,58    Key default "voidframe-theme". Stored value is theme string,
                                             optionally whitelisted by `allowed`. No PII.

src/components/HelpChangelog.tsx:269,282    Key consumer-supplied via `storageKey` prop. Stores the last-
                                            acknowledged version string. No PII.

src/components/Spotlight.tsx:245,254        Key `vf-coachmark:<key>` — stores "1" on coachmark-seen.

src/components/DataGrid.tsx:121,132         Key `vf-datagrid:<key>` — stores column state (widths/hidden/order).
                                            No data content persisted.

── sessionStorage ────────────────────────────────────────────
(none in src/ production code)

── document.cookie ───────────────────────────────────────────
src/utils/cookie.ts:43,78    get/set/delete helpers. Name + value URL-encoded. Exposes optional `secure`,
                             `sameSite`, `path`, `domain`, `maxAge`, `expires`. No secrets stored by the
                             library itself — consumer-driven utility.

Observations:
- Namespace discipline: all library keys prefixed `vf-` or `voidframe-`. Good.
- No secrets/tokens/PII written by library surfaces.
- No localStorage read during render (all reads are in effects or initializers) —
  hydration-safe per useThemePersistence comment block.
- Quota/security errors are caught and swallowed uniformly.
