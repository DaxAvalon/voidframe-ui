# Runtime-message call sites — plan 32 Task 7 Step 1
#
# Every warnOnce(...) / warn(...) / invariant(...) call site in src/ excluding
# __tests__/. Classification key at the bottom.
#
# Format:  file:line  KIND  KEY(if warnOnce)  --  MESSAGE
#
# ── Accessibility label warnings (Form.tsx) ─────────────────────────────
src/components/Form.tsx:56        warn            --  <Input> requires `label`, `aria-label`, or `aria-labelledby` for screen readers.
src/components/Form.tsx:112       warn            --  <Textarea> requires `label`, `aria-label`, or `aria-labelledby` for screen readers.
src/components/Form.tsx:162       warn            --  <Toggle> requires `label`, `aria-label`, or `aria-labelledby` for screen readers.
src/components/Form.tsx:230       warn            --  <Select> requires `label`, `aria-label`, or `aria-labelledby` for screen readers.

# ── Options validity (Form.tsx Select) ──────────────────────────────────
src/components/Form.tsx:235       warnOnce  "Select:empty-options"                 --  Select: `options` is empty — the field will render an empty dropdown.
src/components/Form.tsx:243       warnOnce  `Select:duplicate:${o.value}`          --  Select: duplicate option value "${o.value}" — each option must have a unique `value`.
src/components/Form.tsx:251       warnOnce  `Select:unknown-value:${value}`        --  Select: controlled `value` is "${value}" but no option has that value. The native select will display the first option until `value` matches an option.

# ── Form RadioGroup / FormField (FormExtended.tsx) ──────────────────────
src/components/FormExtended.tsx:146  warnOnce  "RadioGroup:empty-options"           --  RadioGroup: `options` is empty — the group will render nothing. Supply at least one option.
src/components/FormExtended.tsx:154  warnOnce  `RadioGroup:duplicate:${o.value}`    --  RadioGroup: duplicate option value "${o.value}" — each option must have a unique `value`.
src/components/FormExtended.tsx:384  warnOnce  "FormField:required-without-label"   --  FormField: `required` is set but `label` is missing — the asterisk will not render. Provide a `label` or drop `required`.
src/components/FormExtended.tsx:390  warnOnce  "FormField:error-with-help"          --  FormField: both `error` and `help` provided — only `error` renders. Consider combining them if the help text is relevant during errors.

# ── Combobox duplicate options ──────────────────────────────────────────
src/components/Combobox.tsx:106   warnOnce  `Combobox:duplicate:${o.value}`       --  Combobox: duplicate option value "${o.value}" — each option must have a unique `value`.

# ── DataGrid virtualization + groupBy clash ─────────────────────────────
src/components/DataGrid.tsx:1017  warnOnce  "DataGrid:virtual+groupBy"            --  <DataGrid> virtualization is disabled when groupBy is active. Remove groupBy or virtualized to silence this warning.

# ── Tabs (Interactive.tsx) ──────────────────────────────────────────────
src/components/Interactive.tsx:53  warnOnce  `Tabs:duplicate:${t.key}`            --  Tabs: duplicate tab key "${t.key}" — each tab must have a unique `key`.
src/components/Interactive.tsx:61  warnOnce  `Tabs:unknown-active:${active}`      --  Tabs: `active` is "${active}" but no tab has that key. Arrow-key navigation will be inactive until `active` matches a tab.

# ── Modal (Interactive.tsx) a11y ────────────────────────────────────────
src/components/Interactive.tsx:204 warn            --  <Modal> requires a `title` or `aria-label`/`aria-labelledby` for accessibility.

# ── Drawer (Overlay.tsx) a11y + deprecation ─────────────────────────────
src/components/Overlay.tsx:60     warn            --  <Drawer> requires a `title` or `aria-label`/`aria-labelledby` for accessibility.
#   (line 59 also fires a deprecation via deprecatedComponent("Drawer","DrawerV2","v1.1") — see deprecate.ts)

# ── Button asChild ──────────────────────────────────────────────────────
src/components/Button.tsx:69      warn            --  <Button asChild> expects a single React element child (not a plain string).

# ── Primitives (Slot, Transition) ───────────────────────────────────────
src/primitives/Slot.tsx:42        warn            --  <Slot> expects a single React element child. Got: <typeof children>
src/primitives/Transition.tsx:60  warn            --  <Transition> expects a single React element child. Got: <typeof children>

# ── IconButton raw console.warn (bypasses warn() wrapper!) ──────────────
src/icons/IconButton.tsx:63       console.warn    --  [voidframe] <IconButton> requires `aria-label` (or `aria-labelledby`) for screen readers.

# ── safeHref raw console.warn (bypasses warn() wrapper!) ────────────────
src/utils/safeHref.ts:92          console.warn    --  [voidframe] ${component}: rejected unsafe href "${url}" — only http(s), mailto, tel, sms, and relative paths are allowed.

# ── useControllableState (controlled<->uncontrolled flip) ───────────────
src/hooks/useControllableState.ts:42  warnOnce  `controllable-switch:${componentName}`  --  ${who} switched from <was> to <is>. Components must not change between the two during their lifetime.

# ── deprecate.ts helpers ────────────────────────────────────────────────
src/utils/deprecate.ts:15        warnOnce  `deprecated:${component}:${oldName}`    --  <${component}> prop "${oldName}" is deprecated and will be removed in ${version}. Use "${newName}" instead.
src/utils/deprecate.ts:29        warnOnce  `deprecated-component:${component}`     --  <${component}> is deprecated and will be removed in ${version}. Use <${replacement}> instead.

# ── Wrapper itself (src/utils/warn.ts) ──────────────────────────────────
src/utils/warn.ts:85  export function warn(condition, message, ...args)
src/utils/warn.ts:96  export function warnOnce(key, message, ...args)

# ── invariant helper (src/utils/invariant.ts) ───────────────────────────
src/utils/invariant.ts:12  export function invariant(condition, message): asserts condition
#   (no production invariant() call sites in src/ outside of the helper + tests)

# =========================================================================
# Classification summary (details in 32-findings.md §7):
#   GOOD    — WHAT + WHERE + HOW-TO-FIX   : Form.tsx, FormExtended.tsx,
#             Combobox, DataGrid, Tabs (empty/duplicate/unknown-value variants).
#   PARTIAL — WHAT + WHERE, FIX implicit : a11y-label warnings (tell you the
#             rule but not that `hidden` labels via VisuallyHidden also count),
#             useControllableState switch warning (no link to docs).
#   CRYPTIC — none outright; Slot/Transition messages are GOOD for devs but
#             include raw typeof output instead of naming the offending child.
#   DEAD    — none detected.
#
# Inconsistency: IconButton (src/icons/IconButton.tsx:63) and safeHref
# (src/utils/safeHref.ts:92) both bypass the warn()/warnOnce() wrapper and
# call console.warn directly. The README claims "All dev-only warn() and
# warnOnce() calls are guarded by process.env.NODE_ENV !== 'production'".
# These two sites are gated by `NODE_ENV !== 'production'` but do NOT flow
# through the subscribe/emit pipeline that DevPanel's Warnings tab reads, so
# they are invisible in the Dev HUD. See 32-findings.md §7 P1 finding.
#
# Dynamic (template-literal) keys — manual audit:
#   Select:duplicate:${o.value}          (Form.tsx:243)          unique per o.value — safe
#   Select:unknown-value:${value}        (Form.tsx:251)          unique per value  — safe
#   RadioGroup:duplicate:${o.value}      (FormExtended.tsx:154)  unique per value  — safe
#   Combobox:duplicate:${o.value}        (Combobox.tsx:106)      unique per value  — safe
#   Tabs:duplicate:${t.key}              (Interactive.tsx:53)    unique per key    — safe
#   Tabs:unknown-active:${active}        (Interactive.tsx:61)    unique per active — safe
#   controllable-switch:${componentName} (useControllableState.ts:43) one key per comp — safe
#   deprecated:${component}:${oldName}   (deprecate.ts:15)       unique — safe
#   deprecated-component:${component}    (deprecate.ts:29)       unique — safe
#
# Static-key duplicate check (plan methodology note):
#   The plan's grep for duplicate warnOnce keys across static forms does NOT
#   misbehave here — every static key is unique:
#     "Select:empty-options"              (1)
#     "RadioGroup:empty-options"          (1)
#     "FormField:required-without-label"  (1)
#     "FormField:error-with-help"         (1)
#     "DataGrid:virtual+groupBy"          (1)
#   NOTE: the plan's grep pattern combining `-h` flags through `rg`-style
#   piping would have also matched keys inside `__tests__/`. Running the
#   check with a `grep -v __tests__` filter (as elsewhere in the plan) gives
#   the numbers above. No cross-call-site collisions found.
